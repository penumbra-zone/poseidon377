[workspace]

members = [
    "poseidon377",
    # "poseidon-paramgen" # Temporarily excluded until refactored
    "poseidon-permutation",
    "poseidon-consistency",
    "poseidon-parameters",
    "poseidon-tests",
]

[profile.release]
debug = true
